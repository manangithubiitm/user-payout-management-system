# Assumptions

- Each sale belongs to one user.
- Brand names are stored within sales.
- Wallet balance is always synchronized with transactions.
- Commission percentages are fixed according to assignment rules.
- Recovery occurs only once per withdrawal.
- MongoDB ObjectId is used as the primary identifier.